name: INSPECT ZWEAK V4 (ヒント文の あて先)

on:
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: さぎょうツリーを きれいにする
        run: git checkout -- .

      - name: origin/main に おいつく
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git pull --rebase origin main

      - name: しらべる（docs にだけ 書き出す）
        run: python3 scripts/inspect_zweak_v4.py

      - name: src と public が 無傷か たしかめる
        run: |
          git diff --quiet -- src || (echo 'NG: src が 変わっている'; exit 1)
          git diff --quiet -- public || (echo 'NG: public が 変わっている'; exit 1)
          git status --porcelain

      - name: docs だけ コミットして push
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add docs/zweak_probe_v3.txt
          if git diff --cached --quiet; then
            echo 'かわりなし'; exit 0
          fi
          git commit -m 'INSPECT_ZWEAK_V4: probe report v3'
          git pull --rebase origin main
          git push origin HEAD:main
