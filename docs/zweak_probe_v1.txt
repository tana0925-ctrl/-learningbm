LEN src = 955096  html = 5671000
root anchor count = 1
end anchor count = 1
CHAIN = 92
insert-at count = 0
CR in html = False
COUNT LIMITED_DAILY_IDS src= 0 html= 7
COUNT getWarUnitStatsFromMonster src= 0 html= 4
COUNT こわい src= 0 html= 6
COUNT zombieMode src= 1 html= 15
COUNT warState.zombieMode src= 1 html= 9
COUNT __ZWAR_UNLOCK_V1__ src= 6 html= 0
COUNT __ZWEAK_V1__ src= 0 html= 0
COUNT warUnitStats src= 0 html= 0
===== HTML LIMITED_DAILY_IDS #1 pos=3329866 line=11251 =====
"           // 3) 旧コイン監視の基準値が壊れていたら初期化\n            if (!player.oldCoinDailyDate) player.oldCoinDailyDate = getTodayYMD();\n            if (player.oldCoinDailyStart === undefined || player.oldCoinDailyStart === null || isNaN(player.oldCoinDailyStart)) {\n                player.oldCoinDailyStart = player.coins || 0;\n            }\n        }\n\n        // === タナカティーチャー・なしひろし：1日3回制限（4回目から全ステータス1） ===\nconst LIMITED_DAILY_IDS = [152, 153, 999]; // なしひろし, タナカティーチャー, 阪神マン(999)\nconst LIMITED_DAILY_NAME_MAP = {152:'なしひろし',153:'タナカティーチャー',999:'阪神マン'};\nconst LIMITED_DAILY_NAMES = Object.values(LIMITED_DAILY_NAME_MAP);\nconst LIMITED_DAILY_MAX = 3;          // 1日3回まで本気（4回目以降は弱体化）\nconst LIMITED_DAILY_RESET_HOUR = 8;   // 他のデイリー仕様と合わせて朝8時リセット\n\nfunction __dailyPeriodKeyLocal(resetHour){\n    // Prefer the game's canonical 8AM-boundary day key if available\n    try{ if (typeof getDailyPeriodKey8am === 'function') return getDailyPeriodKey8am(); }catch(e){}\n    const h = (typeof resetHour === 'number') ? resetHour : LIMITED_DAILY_RESET_HOUR;\n    const now = new Date();\n    const shifted = new Date(now.getTime()"
===== HTML LIMITED_DAILY_IDS #2 pos=3334209 line=11377 =====
"DAILY_NAME_MAP[Number(id)]; if (n) return n; }catch(e){}\n    try{\n        if (typeof getMonster==='function') {\n            const mm = getMonster(Number(id));\n            if (mm && mm.name && LIMITED_DAILY_NAMES.includes(mm.name)) return mm.name;\n        }\n    }catch(e){}\n    return null;\n}\n\nfunction isLimitedDailyUnit(id, nameHint){\n    const nid = Number(id);\n    return (Number.isFinite(nid) && LIMITED_DAILY_IDS.includes(nid));\n}\n\nfunction getDailyUseCount(id, nameHint){\n    const nid = Number(id);\n    if (!Number.isFinite(nid)) return 0;\n    return __getDluCount(nid);\n}\n\nfunction isDailyFatigued(id, nameHint){\n    const nid = Number(id);\n    if (!Number.isFinite(nid)) return false;\n    // バトル中は、そのバトル開始時のスナップショットを最優先（1回目は本気を保証）\n    try{\n        var __b = (typeof battle !== 'undefined' && battle) ? battle : window.battle;\n        if (__b && __b.__dailyFatigueSnapshot && typeof __b.__dailyFatigueSnapshot[nid] === 'boolean') {\n            return __b.__dailyFatigueSnapshot[nid];\n        }\n    }catch(e){}\n    // それ以外は「すでに本日の使用回数を使っているか」で判定（次回は弱体化するかどうか）\n    return __getDluCount(nid) >= "
===== HTML LIMITED_DAILY_IDS #3 pos=3335148 line=11407 =====
"\n            return __b.__dailyFatigueSnapshot[nid];\n        }\n    }catch(e){}\n    // それ以外は「すでに本日の使用回数を使っているか」で判定（次回は弱体化するかどうか）\n    return __getDluCount(nid) >= LIMITED_DAILY_MAX;\n}\n\nfunction __mirrorDluToPlayer(){\n    try{\n        if (typeof player === 'undefined' || !player) return;\n        const key = __dailyPeriodKeyLocal();\n        const counts = {};\n\n        // mirror known IDs\n        try{ LIMITED_DAILY_IDS.forEach(id => { counts[id] = __getDluCount(id); }); }catch(e){}\n\n        // plus: party-based detection by name/id (robust even if IDs change in future)\n        try{\n            const partyIds = Array.isArray(player.party) ? player.party : [];\n            partyIds.forEach(pid=>{\n                const id = Number(pid);\n                if(!Number.isFinite(id)) return;\n                let mm=null; try{ if (typeof getMonster==='function') mm=getMonster(id); }catch(e){}\n                const nm = mm && mm.name;\n                if (typeof isLimitedDailyUnit==='function' ? isLimitedDailyUnit(id, nm) : LIMITED_DAILY_IDS.includes(id)) {\n                    counts[id] = __getDluCount"
===== HTML LIMITED_DAILY_IDS #4 pos=3335768 line=11417 =====
"sArray(player.party) ? player.party : [];\n            partyIds.forEach(pid=>{\n                const id = Number(pid);\n                if(!Number.isFinite(id)) return;\n                let mm=null; try{ if (typeof getMonster==='function') mm=getMonster(id); }catch(e){}\n                const nm = mm && mm.name;\n                if (typeof isLimitedDailyUnit==='function' ? isLimitedDailyUnit(id, nm) : LIMITED_DAILY_IDS.includes(id)) {\n                    counts[id] = __getDluCount(id);\n                }\n            });\n        }catch(e){}\n\n        player.dailyLimitedUse = { date: key, counts: counts };\n    }catch(e){}\n}\n\nfunction recordLimitedUseForParty(context){\n    if (typeof player === 'undefined' || !player) return;\n\n    // まず、このバトルでの「疲労スナップショット」を作る（増やす前の回数で判定）\n    const partyIds = Array.isArray(player.party) ? player.party.slice() : [];\n    const ids = partyIds.map(x=>Number(x)).filter(n=>Number.isFinite(n));\n\n    const snap = {};\n    const touched = [];\n\n    ids.forEach(id => {\n        let mm=null; try{ if (typeof getMonster==='function') mm=getMonster(id); }catch(e){}\n        cons"
===== HTML LIMITED_DAILY_IDS #5 pos=3336631 line=11440 =====
" = partyIds.map(x=>Number(x)).filter(n=>Number.isFinite(n));\n\n    const snap = {};\n    const touched = [];\n\n    ids.forEach(id => {\n        let mm=null; try{ if (typeof getMonster==='function') mm=getMonster(id); }catch(e){}\n        const nm = (mm && mm.name) ? mm.name : null;\n        const isLimited = (typeof isLimitedDailyUnit==='function') ? isLimitedDailyUnit(id, nm) : (Number.isFinite(id) && LIMITED_DAILY_IDS.includes(id));\n        if (!isLimited) return;\n\n        const usedBefore = __getDluCount(id);\n        const fatiguedThisBattle = usedBefore >= LIMITED_DAILY_MAX;\n        snap[id] = fatiguedThisBattle;\n\n        const after = __incDluCount(id);\n        const dispName = __resolveLimitedName(id, nm) || nm || String(id);\n        touched.push(`${dispName}:${after}/${LIMITED_DAILY_MAX}${fatiguedThisBattle ? '（弱体化）' : ''}`);\n    });\n\n    try{\n        var __b2 = (typeof battle !== 'undefined' && battle) ? battle : window.battle;\n        if (__b2) {\n            __b2.__dailyFatigueSnapshot = {};\n            Object.keys(snap).forEach(k => { __b2.__dailyFatigueSnapshot[Number(k)] = snap"
===== HTML LIMITED_DAILY_IDS #6 pos=4882610 line=38897 =====
"r]) return WAR_ENEMY_POOL_CACHE[tier];\n\n  // tierが上がるほど、参戦候補（ID範囲）を広げる\n  const maxId = Math.min(190, 60 + tier*25); // 60..185（※190まで）\n  const pool = [];\n  for (let id=1; id<=maxId; id++){\n    const m = getMonster(id);\n    if (!m) continue;\n    if (m.isGymLeader) continue;\n    if (m.isWarBoss) continue;\n    if (m.isBoss) continue;\n    if (id >= 900) continue; // 都道府県ボスや先生系は通常敵に混ぜない\n    if (typeof LIMITED_DAILY_IDS !== 'undefined' && LIMITED_DAILY_IDS.includes(id)) continue; // 日替わり制限キャラは通常敵に混ぜない\n    pool.push(id);\n  }\n\n  // 念のためのフォールバック\n  if (pool.length < 20){\n    for (let id=1; id<=150; id++){\n      const m = getMonster(id);\n      if (m) pool.push(id);\n    }\n  }\n  WAR_ENEMY_POOL_CACHE[tier] = pool;\n  return pool;\n}\nfunction warPickEnemyMonsterId(stageIdx, rng){\n  const pool = warGetEnemyPool(stageIdx);\n  const tier = warTierForStage(stageIdx);\n  const targetStage = (tier<=1) ? 1 : (tier<=3 ? 2 : 3); // 後半ほど進化段階が高い敵が出やすい\n\n  // 重み付き抽選（targetStage優遇＋ステータスが高いほど少し重く）\n  let total = 0;\n  const weights = [];\n  for (let i=0; i<pool.length; i++){\n    const id = pool[i];\n    const m = getMons"
===== HTML LIMITED_DAILY_IDS #7 pos=4882647 line=38897 =====
";\n\n  // tierが上がるほど、参戦候補（ID範囲）を広げる\n  const maxId = Math.min(190, 60 + tier*25); // 60..185（※190まで）\n  const pool = [];\n  for (let id=1; id<=maxId; id++){\n    const m = getMonster(id);\n    if (!m) continue;\n    if (m.isGymLeader) continue;\n    if (m.isWarBoss) continue;\n    if (m.isBoss) continue;\n    if (id >= 900) continue; // 都道府県ボスや先生系は通常敵に混ぜない\n    if (typeof LIMITED_DAILY_IDS !== 'undefined' && LIMITED_DAILY_IDS.includes(id)) continue; // 日替わり制限キャラは通常敵に混ぜない\n    pool.push(id);\n  }\n\n  // 念のためのフォールバック\n  if (pool.length < 20){\n    for (let id=1; id<=150; id++){\n      const m = getMonster(id);\n      if (m) pool.push(id);\n    }\n  }\n  WAR_ENEMY_POOL_CACHE[tier] = pool;\n  return pool;\n}\nfunction warPickEnemyMonsterId(stageIdx, rng){\n  const pool = warGetEnemyPool(stageIdx);\n  const tier = warTierForStage(stageIdx);\n  const targetStage = (tier<=1) ? 1 : (tier<=3 ? 2 : 3); // 後半ほど進化段階が高い敵が出やすい\n\n  // 重み付き抽選（targetStage優遇＋ステータスが高いほど少し重く）\n  let total = 0;\n  const weights = [];\n  for (let i=0; i<pool.length; i++){\n    const id = pool[i];\n    const m = getMonster(id) || {};\n    const st = Number("
----- HTML LIMITED_DAILY_IDS shown = 7
===== HTML getWarUnitStatsFromMonster #1 pos=4878293 line=38804 =====
'\'&\':\'&amp;\',\'<\':\'&lt;\',\'>\':\'&gt;\',\'"\':\'&quot;\',"\'":\'&#39;\'}[c])); }\n\n  function getWarUnitStatsFromMonster(mon, id){\n    \ntry{\n  if (typeof isDailyFatigued === \'function\' && isDailyFatigued(id)) {\n    const m3 = (mon && Array.isArray(mon.moves) && mon.moves[2]) ? mon.moves[2] : null;\n    const skill3Name = m3 ? (m3.name||\'\') : \'\';\n    const skill3Effect = m3 ? (m3.effect||\'\') : \'\';\n    const spd = 1;\n    const hp = 1, atk = 1, def = 1;\n    const move = 9 + clamp(spd,5,200)/10;\n    const cost = clamp(Math.round((hp*0.55 + atk*4 + def*0.8 + spd*0.5)/6), 30, 160);\n    const atkInterval = clamp(1000 - spd*4, 380, 1000);\n    return { kind:\'monster\', id, name: mon.name||\'モンスター\', sprite: mon.sprite||\'❓\', buff:(mon.buff||\'attack\'), elementType:(mon.elementType||\'normal\'),\n      hpMax:hp, hp, atk, def, move, atkInterval, cost,\n      skill3Name, skill3Effect,\n      desc:`（疲れ）体力1 / こうげき1` };\n  }\n}catch(e){}\nlet lv = 1;\n    try{ lv = (player && player.monsters && player.monsters[id] && player.monsters[id].level) ? player.monsters[id].level : 1; }catch(e){}\n    const scale = 1 + Math.min(0.8, (lv-1)*0.03);\n    let hp = Math.round((mon.hp || 100) * 0.35 * scale);\n    let atk = Math.round((mon.atk || 20) * 0.45 * scale);\n    let spd = (mon.spd != null) ? Number(mon.spd||30) : ((typeof getStats===\'function\') ? Number((getStats(mon, lv)||{}).spd||30) : 30);\n    let def = (mon.def || 20);\n    try{\n      const et = (mon.elementType||\'normal\');\n      const c = (typeof hidenBonusCounts===\'function\') ? hidenBonusCounts(id, et) : {hp:0,atk:0,def:0,spd:0};\n      hp  = Math.round(hp  * (1 + (c.hp||0)*0.01));\n      atk = Math.round(atk * (1 + (c.atk||0)*0.01));\n      def = Math.round(def * (1 + (c.def||0)*0.01));\n      spd = Math.round(spd * (1 + (c.spd||0)*0.01));\n    }catch(e){}\n    // ★星ランク補正（攻略モード）\n    try{\n      const mul = getStarMultiplier(id);\n      if(mul !== 1){\n        hp = Math.round(hp * mul);\n        atk = Math.round(atk * mul);\n        def = Math.round(def * mul);\n        spd = Math.round(spd * mul);\n      }\n    }catch(e){}\n    const m3 = (mon && Array.isArray(mon.moves) && mon.moves[2]) ? mon.moves[2] : null;\n    const skill3Name = m3 ? (m3.name||\'\') : \'\';\n    const skill3Effect = m3 ? (m3.effect||\'\') : \'\';\n    const move = 5 + clamp(spd,5,200)/14;  '
===== HTML getWarUnitStatsFromMonster #2 pos=4901772 line=39466 =====
"typeof getMonster==='function') ? getMonster(id) : null;\n      const st = mon ? getWarUnitStatsFromMonster(mon, id) : {cost:999, hpMax:200, atk:20};\n\n      const cost = Number(st.cost||999);\n      const cd = Math.max(0, Number(ps.slotCd[i]||0));\n      const cdMax = Math.max(0.001, Number(ps.slotCdMax[i]||calcRechargeSec(st)||3));\n      const globalCd = Math.max(0, Number(ps.globalSpawnCd||0));\n\n      const ok = (ps.power >= cost) && cd <= 0 && globalCd <= 0 && !atLimit;\n      wrap.style.opacity = ok ? 1 : 0.55;\n      wrap.disabled = !ok;\n\n      const cover = document.getElementById('warCdCover'+i);\n      const text  = document.getElementById('warCdText'+i);\n      const bar   = document.getElementById('warCdBar'+i);\n      const hint  = document.getElementById('warTapHint'+i);\n\n      // 表示制御\n      if(atLimit){\n        if(cover){ cover.classList.remove('hidden'); cover.classList.add('flex'); }\n        if(text) text.textContent = '上限';\n        if(hint) hint.textContent = '場がいっぱい';\n      }else if(cd > 0){\n        if(cover){ cover.classList.remove('hidden'); cover.classList.add('flex'); }\n        if(text) text.textContent = `待ち ${cd.toFixed(1)}s`;\n        if(hint) hint.textContent = '再出撃待ち';\n      }else if(globalCd > 0){\n        if(cover){ cover.classList.remove('hidden'); cover.classList.add('flex'); }\n        if(text) text.textContent = `クール ${globalCd.toFixed(1)}s`;\n        if(hint) hint.textContent = 'クール中';\n      }else if(ps.power < cost){\n        if(cover){ cover.classList.remove('hidden'); cover.classList.add('flex'); }\n        if(text) text.textContent = `⚡${cost}`;\n        if(hint) hint.textContent = 'パワー不足';\n      }else{\n        if(cover){ cover.classList.add('hidden'); cover.classList.remove('flex'); }\n        if(hint) hint.textContent = 'タップで出陣';\n      }\n\n      if(bar){\n        const remain = (cd>0) ? (cd/cdMax) : 0;\n        const pct = clamp((1-remain)*100, 0, 100);\n        bar.style.width = pct + '%';\n      }\n    }\n\n\n    // 学習パワー上限アップ（4つ目カード→5つ目に移動）の状態更新\n    const capBtn = document.getElementById('warPowerCapBtn');\n    if(capBtn){\n      const up = (player && player.warPowerCapUp) ? Number(player.warPowerCapUp||0) : 0;\n      const maxUp = 10;\n      const canMore = up < maxUp;\n      const cost = warPowerCapUpgradeCost(up);\n      const"
===== HTML getWarUnitStatsFromMonster #3 pos=4914660 line=39811 =====
'typeof getMonster===\'function\') ? getMonster(id) : null;\n      const st = mon ? getWarUnitStatsFromMonster(mon, id) : {cost:60, name:\'?\', sprite:\'❓\', hpMax:200, atk:20};\n      const isZombieForbidden = !!(warState && warState.zombieMode) && (id===152 || id===153 || id===999 || (mon && (mon.name===\'タナカティーチャー\' || mon.name===\'なしひろし\')));\n      const cdMax = calcRechargeSec(st);\n      ps.slotCdMax[i] = cdMax;\n      if(typeof ps.slotCd[i] !== \'number\') ps.slotCd[i] = 0;\n      box.innerHTML += `\n        <button id="warSpawnBtn${i}" onclick="warSpawnUnit(${i})" class="relative overflow-hidden bg-white rounded-xl p-3 border-2 border-pink-200 shadow hover:shadow-lg transition active:translate-y-0.5 ${isZombieForbidden ? \'opacity-60 grayscale cursor-not-allowed\' : \'\'}">\n          <div class="absolute top-2 right-2 text-[10px] font-black bg-yellow-100 text-yellow-800 px-2 py-1 rounded-full border border-yellow-200">⚡${Math.round(st.cost||0)}</div>\n          <div class="text-3xl leading-none">${escapeHtml(st.sprite||\'❓\')}</div>\n          <div class="text-xs font-black text-gray-800 mt-1 truncate">${escapeHtml(st.name)}</div>\n          <div class="text-[10px] text-gray-500 font-bold line-clamp-1">${escapeHtml(st.desc||\'\')}</div>\n          <div class="mt-2 h-1 w-full bg-gray-200 rounded">\n            <div id="warCdBar${i}" class="h-1 bg-green-500 rounded" style="width:100%"></div>\n          </div>\n          <div class="mt-2 text-[10px] font-black text-pink-700 border border-pink-200 rounded-lg py-1 text-center" id="warTapHint${i}">${isZombieForbidden ? \'こわい\' : \'タップで出陣\'}</div>\n          <div id="warCdCover${i}" class="absolute inset-0 rounded-xl bg-white/80 hidden flex items-center justify-center">\n            <div class="text-xs font-black text-gray-800" id="warCdText${i}">待ち 0.0s</div>\n          </div>\n        </button>`;\n    }\n    \n    // 4つ目(旧)：学習パワーを使って「上限アップ」\n    box.innerHTML += `\n      <button id="warPowerCapBtn" onclick="warUpgradePowerCap()" class="relative bg-white rounded-xl border-2 border-yellow-200 shadow hover:shadow-lg transition active:translate-y-0.5">\n        <div class="absolute top-2 right-2 text-[10px] font-black bg-yellow-100 text-yellow-800 px-2 py-1 rounded-full border border-yellow-200">⚡<span id="warPowerCapCostBadge">0</span></'
===== HTML getWarUnitStatsFromMonster #4 pos=4924921 line=40016 =====
"     else { try{ alert('こわい'); }catch(e){} }\n      return;\n    }\n    const st = getWarUnitStatsFromMonster(mon, id);\n    if(ps.power < st.cost) return;\n    ps.power -= st.cost;\n    renderPower();\n    spawnUnit('player', st);\n    try{ if(!ps.deployedIds) ps.deployedIds = {}; ps.deployedIds[id] = true; }catch(e){}\n    // グローバルクール（連打防止）\n    ps.globalSpawnCd = 0.25;\n    // スロットごとの再出撃待ち\n    const cdMax = ps.slotCdMax[slot] || calcRechargeSec(st);\n    ps.slotCd[slot] = cdMax;\n    updateSpawnButtonState();\n  };\n\n  function spawnEnemy(){\n    const es = warState;\n    const enemyAlive = es.units.filter(u=>u.team==='enemy' && u.hp>0).length;\n    if(enemyAlive >= (es.maxEnemyUnits||12)) return;\n    const hpLoss = clamp(1 - (es.enemyCastleHp/Math.max(1,es.enemyCastleHpMax)), 0, 1);\n    const diff = (es.elapsed/6) + (Math.max(0,(es.wave||1)-1))*1.2 + hpLoss*6;\n    spawnUnit('enemy', getEnemyTemplate(diff));\n  }\n\n  function nearestEngagement(){\n    const players = warState.units.filter(u=>u.team==='player' && u.hp>0).sort((a,b)=>a.x-b.x);\n    const enemies = warState.units.filter(u=>u.team==='enemy' && u.hp>0).sort((a,b)=>a.x-b.x);\n    warState.units.forEach(u=>{ if(u.target && u.target.hp<=0) u.target=null; });\n\n    for(const pu of players){\n      if(pu.target) continue;\n      let cand=null, best=999;\n      for(const en of enemies){\n        const dist = Math.abs(pu.x-en.x);\n        if(en.x < pu.x && dist < best){ best=dist; cand=en; }\n      }\n      if(cand && best < 6){ pu.target=cand; cand.target=pu; }\n    }\n  }\n\n  function num(v, fb){ v = Number(v); return Number.isFinite(v) ? v : fb; }\nfunction fin(v){ v = Number(v); return Number.isFinite(v); }\n\n// --- 攻略モード：技3（バフ/デバフ/回復）を確率で発動 ---\nfunction warNow(){ return Date.now(); }\nfunction warFloatText(xPct, bottomPct, text){\n  const field = document.getElementById('warField');\n  if(!field) return;\n  const t = document.createElement('div');\n  t.className = 'war-float-text';\n  t.textContent = String(text||'');\n  t.style.left = (xPct||50)+'%';\n  t.style.bottom = (bottomPct||22)+'%';\n  field.appendChild(t);\n  setTimeout(()=>{ try{ t.remove(); }catch(e){} }, 920);\n}\nfunction warAtkMul(u, now){\n  let m = 1;\n  if(u && u.bAtkUntil && now < u.bAtkUntil) m *= (u.bAtkMul||1.25);\n  if(u && u.dAtkUntil && now < u.dAtkUnti"
----- HTML getWarUnitStatsFromMonster shown = 4
===== HTML KOWAI #1 pos=3150617 line=6604 =====
"'まず左でしっかり' },\n        { name: '右分数スラッシュ', type: 'heavy',  pow: 46, acc: 0.73, element: 'normal', desc: '端数が地味に痛い' },\n        { name: '帯でまとめる',   type: 'unique', pow: 0,  acc: 1.00, effect: 'buff_def_party', desc: '味方全体の防御UP（整列完了）' }\n    ]\n});\n\n// === ラボ事故キャラ：ミユウツー（ラボ復活の失敗でムダにした「かけら」50個ごとに出現）===\nconst MEWTWO_ID = 1045;\nMONSTERS.push({\n    id: MEWTWO_ID,\n    name: 'ミユウツー',\n    sprite: '🧠',\n    hp: 380, atk: 135, def: 75, spd: 85,\n    buff: 'attack',\n    elementType: 'psychic',\n    stage: 2,\n    gacha: false,\n    nextId: null,\n    evoLevel: null,\n    desc: '研究室の奥で腕組みして待っている。小数のわり算が遅いと、無言の圧が飛んでくる。こわいのは問題じゃなくて視線。',\n    skills: [\n        { name: 'サイコスラッシュ', type: 'normal', pow: 26, acc: 0.95, element: 'psychic', desc: '脳内で切ってる（怖）' },\n        { name: 'ラボ・バースト', type: 'heavy', pow: 55, acc: 0.75, element: 'psychic', desc: '研究室の空気が爆ぜる' },\n        "
===== HTML KOWAI #2 pos=3912914 line=22503 =====
'ED" },\n    { idiom: "腹を割る", meaning: "本心を打ち明ける", dummies: ["相手をだます", "話をそらす", "秘密にする"], sentence: "友だちと（\u3000\u3000）話し合った。", slotType: "PRED" },\n    { idiom: "頭を抱える", meaning: "困って悩む", dummies: ["すぐに決める", "自信満々になる", "何も気にしない"], sentence: "問題が難しすぎて（\u3000\u3000）。", slotType: "PRED" },\n    { idiom: "手が回らない", meaning: "忙しくて対応できない", dummies: ["時間がたっぷりある", "すぐに終わる", "余裕がありすぎる"], sentence: "忙しくて片付けまで（\u3000\u3000）。", slotType: "PRED" },\n    { idiom: "胸を張る", meaning: "自信をもつ", dummies: ["元気がなくなる", "こそこそする", "謝り続ける"], sentence: "努力の結果が出たので（\u3000\u3000）。", slotType: "PRED" },\n    { idiom: "顔を赤らめる", meaning: "恥ずかしくなる", dummies: ["眠くなる", "怒ってこわい顔になる", "病気で倒れる"], sentence: "ほめられて思わず（\u3000\u3000）。", slotType: "PRED" },\n    { idiom: "肩の荷が下りる", meaning: "心配がなくなりほっとする", dummies: ["さらに不安になる", "大失敗する", "急に怒られる"], sentence: "大事な仕事が終わって（\u3000\u3000）。", slotType: "PRED" },\n    { idiom: "目を光らせる", meaning: "注意して見張る",'
===== HTML KOWAI #3 pos=4916145 line=39825 =====
'class="text-3xl leading-none">${escapeHtml(st.sprite||\'❓\')}</div>\n          <div class="text-xs font-black text-gray-800 mt-1 truncate">${escapeHtml(st.name)}</div>\n          <div class="text-[10px] text-gray-500 font-bold line-clamp-1">${escapeHtml(st.desc||\'\')}</div>\n          <div class="mt-2 h-1 w-full bg-gray-200 rounded">\n            <div id="warCdBar${i}" class="h-1 bg-green-500 rounded" style="width:100%"></div>\n          </div>\n          <div class="mt-2 text-[10px] font-black text-pink-700 border border-pink-200 rounded-lg py-1 text-center" id="warTapHint${i}">${isZombieForbidden ? \'こわい\' : \'タップで出陣\'}</div>\n          <div id="warCdCover${i}" class="absolute inset-0 rounded-xl bg-white/80 hidden flex items-center justify-center">\n            <div class="text-xs font-black text-gray-800" id="warCdText${i}">待ち 0.0s</div>\n          </'
===== HTML KOWAI #4 pos=4924793 line=40012 =====
"   if(fb){ fb.textContent = '🚫 これ以上出せない！（出撃上限）'; fb.className='font-bold text-yellow-200'; }\n      return;\n    }\n    const p = (typeof player==='object' && player && Array.isArray(player.party)) ? player.party.slice(0,3) : [];\n    const id = p[slot];\n    if(id == null) return;\n    const mon = (typeof getMonster==='function') ? getMonster(id) : null;\n    if(!mon) return;\n    if(warState && warState.zombieMode && (id===152 || id===153 || id===999 || (mon && (mon.name==='タナカティーチャー' || mon.name==='なしひろし')))){\n      const fb = document.getElementById('warFeedback');\n      if(fb){ fb.textContent = 'こわい'; fb.className='font-bold text-red-200'; }\n      else { try{ alert('こわい'); }catch(e){} }\n      return;\n    }\n    const st = getWarUnitStatsFromMonster(mon, id);\n    if(ps.power < st.cost) return;\n    ps.power -= st.cost;\n    renderPower();\n    sp"
===== HTML KOWAI #5 pos=4924865 line=40013 =====
" text-yellow-200'; }\n      return;\n    }\n    const p = (typeof player==='object' && player && Array.isArray(player.party)) ? player.party.slice(0,3) : [];\n    const id = p[slot];\n    if(id == null) return;\n    const mon = (typeof getMonster==='function') ? getMonster(id) : null;\n    if(!mon) return;\n    if(warState && warState.zombieMode && (id===152 || id===153 || id===999 || (mon && (mon.name==='タナカティーチャー' || mon.name==='なしひろし')))){\n      const fb = document.getElementById('warFeedback');\n      if(fb){ fb.textContent = 'こわい'; fb.className='font-bold text-red-200'; }\n      else { try{ alert('こわい'); }catch(e){} }\n      return;\n    }\n    const st = getWarUnitStatsFromMonster(mon, id);\n    if(ps.power < st.cost) return;\n    ps.power -= st.cost;\n    renderPower();\n    spawnUnit('player', st);\n    try{ if(!ps.deployedIds) ps.deployedIds = {};"
===== HTML KOWAI #6 pos=5504855 line=52233 =====
"qR('馬','うま',['うし','いぬ','ねこ']),_kqR('鳥','とり',['さかな','むし','うま']),_kqR('黒','くろ',['しろ','あか','あお']),_kqR('強','つよい',['よわい','おおきい','はやい']),_kqR('弱','よわい',['つよい','ちいさい','おそい']),_kqR('遠','とおい',['ちかい','たかい','ながい']),_kqR('近','ちかい',['とおい','ひくい','みじかい']),_kqR('新','あたらしい',['ふるい','おおきい','ちいさい']),_kqR('古','ふるい',['あたらしい','わかい','たかい']),_kqR('長','ながい',['みじかい','たかい','ひくい']),_kqR('高','たかい',['ひくい','ながい','おもい']),_kqR('太','ふとい',['ほそい','ながい','みじかい'])],\nk3:[_kqR('意','い',['し','ち','き']),_kqR('島','しま',['うみ','やま','かわ']),_kqR('港','みなと',['えき','くうこう','まち']),_kqR('暗','くらい',['あかるい','あつい','さむい']),_kqR('悲','かなしい',['うれしい','たのしい','こわい']),_kqR('集','あつまる',['ちる','ならぶ','はしる']),_kqR('落','おちる',['あがる','のぼる','とぶ']),_kqR('進','すすむ',['もどる','さがる','とまる']),_kqR('送','おくる',['もらう','わたす','うける']),_kqR('開','ひらく',['しめる','あける','とじる']),_kqR('起','おきる',['ねる','とまる','すわる']),_kqR('寒','さむい',['あつい','すずしい',"
----- HTML KOWAI shown = 6
----- SRC LIMITED_DAILY_IDS shown = 0
----- SRC KOWAI shown = 0
INSPECT DONE
